# 대여 노트북 반납 전 정리 목록

이 프로젝트를 GitHub에 올리기 위해 로그인하거나 토큰을 사용했다면, 노트북 반납 전에 아래 항목을 정리합니다.

## 1. GitHub 인증 정보 삭제

1. Windows 시작 메뉴에서 **자격 증명 관리자**를 엽니다.
2. **Windows 자격 증명** 또는 **일반 자격 증명**에서 GitHub 관련 항목을 찾습니다.
3. `git:https://github.com`처럼 표시된 GitHub 자격 증명을 삭제합니다.

## 2. GitHub 토큰 폐기

GitHub 웹사이트에서 다음 경로로 이동합니다.

**프로필 사진 → Settings → Developer settings → Personal access tokens**

이 작업을 위해 만든 Personal Access Token은 **Revoke**합니다. 컴퓨터에서 삭제하는 것만으로는 토큰 자체가 무효화되지 않으므로, 이 단계가 가장 중요합니다.

## 3. 브라우저 로그인 정리

- GitHub에서 로그아웃합니다.
- 사용한 브라우저에서 GitHub의 쿠키와 사이트 데이터를 삭제합니다.
- 가능하면 사용한 브라우저 프로필도 로그아웃하거나 제거합니다.

## 4. SSH 키를 만들었을 때만

이번 프로젝트 작업에서는 SSH 키를 만들지 않았습니다. 나중에 직접 만들었다면 `C:\\Users\\<사용자>\\.ssh` 폴더의 해당 개인 키와 공개 키를 삭제하고, GitHub Settings의 SSH keys에서도 해당 키를 제거합니다.

## 참고

- 이 프로젝트의 Git 원격 주소에는 토큰이 포함되어 있지 않습니다.
- Git의 사용자 이름과 이메일은 비밀 정보는 아닙니다. 원한다면 이 프로젝트에서 별도로 제거할 수 있습니다.
- 토큰, 비밀번호, 인증 코드는 이 문서나 소스 코드에 기록하지 않습니다.
